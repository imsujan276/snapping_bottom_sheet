name: snapping_bottom_sheet
description: Draggable, Scrobllable and Snapping Bottom Sheet.
version: 1.0.1
homepage: https://github.com/imsujan276/snapping_bottom_sheet

screenshots:
 - description: 'Example of the snapping bottom sheet'
   path: example.gif

environment:
  sdk: '>=2.19.3 <4.0.0'
  flutter: ">=1.17.0"

dependencies:
  flutter:
    sdk: flutter

dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_lints: ^2.0.0

flutter:
